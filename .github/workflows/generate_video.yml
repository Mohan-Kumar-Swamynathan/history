# DISABLED — replaced by thulir_unified.yml
# Manual trigger only for debugging.

name: Thulir Daily Content (deprecated)

on:
  workflow_dispatch:
    inputs:
      format:
        description: "Video format"
        required: false
        default: "long"
        type: choice
        options:
          - long
          - short

jobs:
  deprecated_notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is deprecated. Use 'Thulir Unified Pipeline' instead."
          exit 1
